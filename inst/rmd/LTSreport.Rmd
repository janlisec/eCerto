---
title: "Report --Long Term Stability--"
output: pdf_document
params:
  c: NA
  dat: NA
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

## R Markdown

This is a report about long term stability by Bundesanstalt für Materialforschung.

```{r pre-calculation of month-value data frame}
x = params$dat
vals <- x[["val"]][,"Value"]
rt <- x[["val"]][,"Date"]
mon <- sapply(rt, function(x) { mondf(d_start = rt[1], d_end = x) })
d = data.frame(mon, vals)
```


```{r}
plot_lts_data(x = params$dat, type=1)
if(sum(!is.na(params$c))>=1){
  points(x = d[!is.na(params$c),"mon"],y = d[!is.na(params$c),"vals"], pch=24, bg="red")
} 
```


```{r}
plot_lts_data(x = params$dat, type=2)
if(sum(!is.na(params$c))>=1){
  points(x = d[!is.na(params$c),"mon"],y = d[!is.na(params$c),"vals"], pch=24, bg="red")
} 
```

## Summary

```{r}
knitr::kable(params$dat[["def"]])
```

## Statistical Information Monitoring:

```{r}
length(params$dat)
```


