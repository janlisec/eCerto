default:
  golem_name: eCerto
  app_version: !expr as.character(utils::packageVersion("eCerto"))
  app_date: !expr as.character(utils::packageDate("eCerto"))
  app_prod: no
  silent: yes
  bam_server: yes
production:
  app_prod: yes
dev:
  silent: no
  app_version: development
  app_date: !expr as.character(Sys.Date())
